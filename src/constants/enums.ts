export enum AUTH_TYPE {
  SIGN_IN = 'signIn',
  SIGN_UP = 'signUp',
}

export enum AUTH_INPUT_NAMES {
  EMAIL = 'email',
  USERNAME = 'username',
  PASSWORD = 'password',
  CONFIRM_PASSWORD = 'confirmPassword',
}
